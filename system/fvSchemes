/*--------------------------------*- C++ -*----------------------------------*\
| =========                 |                                                 |
| \\      /  F ield         | OpenFOAM: The Open Source CFD Toolbox           |
|  \\    /   O peration     | Version:  2.1.1                                 |
|   \\  /    A nd           | Web:      www.OpenFOAM.org                      |
|    \\/     M anipulation  |                                                 |
\*---------------------------------------------------------------------------*/
FoamFile
{
    version     2.0;
    format      ascii;
    class       dictionary;
    location    "system";
    object      fvSchemes;
}
// * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * * //

fluxScheme      Kurganov;

ddtSchemes
{
    default         Euler;
}

gradSchemes
{
   // default         Gauss linear;
    default        cellLimited Gauss linear 1.0;
}

divSchemes
{
    default         Gauss linear;
    div(tauMC)      Gauss linear;
}

laplacianSchemes
{
  //  default         Gauss linear corrected;
 // default        Gauss linear limited 0.33;

default        Gauss linear orthogonal;

grad(alphaL)  leastSquare;
}

interpolationSchemes
{
    default         linear;
  /*  reconstruct(rho) vanLeer;
    reconstruct(U)  vanLeerV;
    reconstruct(T)  vanLeer;*/
   // reconstruct(rho) ROUNDAplus;
   // reconstruct(U)  ROUNDAplusV;
   // reconstruct(T)  ROUNDAplus;
   // reconstruct(Yi) ROUNDAplus01;
   
  /* reconstruct(rho) upwind;
   reconstruct(U)  upwind;
   reconstruct(T)  upwind;
   reconstruct(p)  upwind;
   reconstruct(Y0) upwind;
   reconstruct(Y1) upwind;
   reconstruct(Y2) upwind;*/
   
  /* reconstruct(rho) ROUNDAplus;
   reconstruct(U)  ROUNDAplusV;
   reconstruct(T)  ROUNDAplus;
   reconstruct(p)  ROUNDAplus;
   reconstruct(Y0) ROUNDAplus01;
   reconstruct(Y1) ROUNDAplus01;
   reconstruct(Y2) ROUNDAplus01;*/
   
   reconstruct(rho) ROUNDA;
   reconstruct(U)  ROUNDAV;
   reconstruct(T)  ROUNDA;
   reconstruct(p)  ROUNDA;
   reconstruct(Y0) ROUNDA01;
   reconstruct(Y1) ROUNDA01;
   reconstruct(Y2) ROUNDA01;
   
  /* reconstruct(rho) ROUNDF;
   reconstruct(U)  ROUNDF;
   reconstruct(T)  ROUNDF;
   reconstruct(p)  ROUNDF;
   reconstruct(Y0) ROUNDF01;
   reconstruct(Y1) ROUNDF01;
   reconstruct(Y2) ROUNDF01;*/
   limitedScheme linear;
    
}

snGradSchemes
{
//    default         limited 0.33;
default         orthogonal;
}

fluxRequired
{
    default         no;
    p;
}

// ************************************************************************* //
